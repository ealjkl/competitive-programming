#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ii pair<int, int>
#define vi vector<int>
#define fi first
#define se second
#define endl '\n'
#define debug(x) cout << #x << ": " << x << endl

pair<int, int> findX (string s) {

} 

void solve() {
    string s; 
    cin >> s;
    int cnt = s.find("ff", 2);
    int x = 1;
    while(x != 0) {
        
    }
}

signed main() {
    int t; cin >> t;
    while (t--) {
        solve();        
    }
}