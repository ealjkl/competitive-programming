#include <iostream>
#include <utility> 
#include <map> 
#include <utility>

using namespace std;
int main() {
   pair<int, int> indexStart = {1, 2}; 
   pair<int, int> indexEnd = {3, 4}; 
   indexStart = {5, 6};
   map<int, int> mapping;
   mapping.insert(pair<int, int>(1, 40));
   cout << mapping.;
}