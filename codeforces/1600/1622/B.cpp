#include <bits/stdc++.h>
using namespace std;
#define int long long
#define ll long long
#define ii pair<ll, ll>
#define vi vector<ll>
#define fi first
#define se second
#define endl '\n'
#define debug(x) cout << #x << ": " << x << endl

void solve() {
    int n; cin >> n;
    vi p(n), q;
    string likes;
    for (int i = 0; i < n; i++) {
        cin >> p[i];
    }
    cin >> likes;
}

signed main() {
    int t; cin >> t;
    while (t--) {
    }
}