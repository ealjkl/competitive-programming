export default class CppFileMerger {
    private readonly parser;
    private readonly includeDirectory;
    private readonly sourceDirectory;
    private readonly systemIncludes;
    private readonly processedOnce;
    private workingDirectory;
    constructor(params?: {
        includeDirectory?: string;
        sourceDirectory?: string;
    });
    parse(filePath: string): string;
    private parseFile;
    private parseIncludedFile;
    private parseSourceFiles;
    private getHeaderRelativePath;
}
